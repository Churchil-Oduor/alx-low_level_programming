#include "main.h"

/**
 * print_last_digit - fx that prints the last digit
 * @n: digit whose last digit is sought
 * Return: Returns the last digit of a number
 */

int print_last_digit(int n)
{
	return (n % 10);
}
