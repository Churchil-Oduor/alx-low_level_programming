#include "main.h"

/**
 * _abs - computes absolute
 * @n: integer entered
 * Return: Returns the absolute integer value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
