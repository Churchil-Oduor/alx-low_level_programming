#include "main.h"
/**
 * mul - takes in two integers and multiplies them together.
 * @a: first integer entered.
 * @b: second integer entered.
 * Return: returns the product of the two integers entered.
 */
int mul(int a, int b)
{
	return (a * b);
}
