#include "main.h"

/**
 * mul - fx that multiplies two params passed to it
 * @a: first param
 * @b: second param
 * Return: Returns the product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
