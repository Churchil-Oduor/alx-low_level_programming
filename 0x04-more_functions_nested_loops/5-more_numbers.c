#include "main.h"
/**
 * more_numbers - prints number 0 to 14 ten times followed by a new line.
 * Return: void function.
 */
void more_numbers(void)
{
	int counter, num;

	counter, num = 0;
	while (count < 10)
	{
		while (num++ < 
	}
}
